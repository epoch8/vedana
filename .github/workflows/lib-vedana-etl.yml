# This file was automatically generated by uv-workspace-codegen
# For more information, see: https://github.com/epoch8/uv-workspace-codegen/blob/master/README.md
# Do not edit this file manually - changes will be overwritten

name: Test lib vedana-etl

on:
  pull_request:
    paths:
      - "libs/vedana-etl/**"
  push:
    branches: [master]
    paths:
      - "libs/vedana-etl/**"
      - "pyproject.toml"
      - "uv.lock"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v6

      - name: Install
        shell: bash
        run: |
          cd libs/vedana-etl
          uv sync --all-groups

      - name: Start infra (Grist + Memgraph)
        run: 'docker compose -f libs/vedana-etl/tests/infra/docker-compose.ci.yml up -d

            echo "Waiting containers..."

            docker ps

            '


      - env:
            EMBEDDINGS_DIM: '1536'
            GRIST_API_KEY: e30d2f274a538c05fecd14510887f8a3b7eab718
            GRIST_DATA_DOC_ID: qAxQ1gcBKcW7
            GRIST_DATA_MODEL_DOC_ID: wEEmPY3UiwMD
            GRIST_DEFAULT_EMAIL: ci@example.com
            GRIST_ORG_IN_PATH: 'true'
            GRIST_SERVER_URL: http://0.0.0.0:8484
            GRIST_SESSION_SECRET: dev-secret
            MEMGRAPH_PWD: ''
            MEMGRAPH_URI: bolt://localhost:7687
            MEMGRAPH_USER: ''
        name: Run unit + integration tests
        run: 'uv run pytest libs/vedana-etl/

            '


      - name: Check formatting
        shell: bash
        run: |
          uv run ruff check libs/vedana-etl

      - name: Check typing
        shell: bash
        run: |
          uv run mypy -p vedana_etl