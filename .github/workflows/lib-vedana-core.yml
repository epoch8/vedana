# This file was automatically generated by uv-workspace-codegen
# For more information, see: https://github.com/epoch8/uv-workspace-codegen/blob/master/README.md
# Do not edit this file manually - changes will be overwritten

name: Test lib vedana-core

on:
  pull_request:
    paths:
      - "libs/vedana-core/**"
      # - "pyproject.toml"
      # - "uv.lock"
  push:
    branches: [master]
    paths:
      - "libs/vedana-core/**"
      - "pyproject.toml"
      - "uv.lock"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v6

      - name: Install
        shell: bash
        run: |
          cd libs/vedana-core
          uv sync --all-groups

      - env:
            DB_CONN_URI: 'sqlite:///:memory:'
            EMBEDDINGS_DIM: '1024'
            GRIST_API_KEY: e30d2f274a538c05fecd14510887f8a3b7eab718
            GRIST_DATA_DOC_ID: qAxQ1gcBKcW7
            GRIST_DATA_MODEL_DOC_ID: wEEmPY3UiwMD
            GRIST_SERVER_URL: http://0.0.0.0:8484
            MEMGRAPH_PWD: ''
            MEMGRAPH_URI: bolt://localhost:7687
            MEMGRAPH_USER: ''
            OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        name: Run tests
        run: 'uv run pytest libs/vedana-core/

            '


      - name: Check formatting
        shell: bash
        run: |
          uv run ruff check libs/vedana-core

      - name: Check typing
        shell: bash
        run: |
          uv run mypy -p vedana_core
