# This file was automatically generated by uv-workspace-codegen
# For more information, see: https://github.com/epoch8/uv-workspace-codegen/blob/master/README.md
# Do not edit this file manually - changes will be overwritten

name: Test lib jims-tui

on:
  pull_request:
    paths:
      - "libs/jims-tui/**"
      # - "pyproject.toml"
      # - "uv.lock"
  push:
    branches: [master]
    paths:
      - "libs/jims-tui/**"
      - "pyproject.toml"
      - "uv.lock"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v6

      - name: Install
        shell: bash
        run: |
          cd libs/jims-tui
          uv sync --all-groups

      - name: Check formatting
        shell: bash
        run: |
          uv run ruff check libs/jims-tui

      - name: Check typing
        shell: bash
        run: |
          uv run mypy -p jims_tui